<%= form_with model: current_user, local: true do |f| %>

  <%= f.label :nickname, value: current_user.nickname %>
  <%= f.text_field :nickname %>

  <%= f.submit 'Aktualizuj konto!' %>

<% end %>

<%= link_to 'ZMIEŃ HASŁO', change_passwords_path, method: :post %>

<%= link_to '< Wróć', boards_path %>
