<%= link_to 'Nowy board', new_board_path %>

<% @boards.each do |board| %>

  <%= link_to "#{board.title}", board_path(board) %>
  <%= link_to "Usuń", board_path(board), method: :delete, data: { confirm: 'Jesteś pewny, że chcesz usunąć ten BOARD?'} %>
  <%= link_to "Edytuj", edit_board_path(board) %>

<% end %>

  <%= link_to '< WRÓĆ', root_path %>
